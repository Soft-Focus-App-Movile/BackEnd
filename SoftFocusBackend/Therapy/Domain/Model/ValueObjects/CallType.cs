namespace SoftFocusBackend.Therapy.Domain.Model.ValueObjects
{
    /// <summary>
    /// Media type of a call. Audio = voice only, Video = audio + video.
    /// </summary>
    public enum CallType
    {
        Audio,
        Video
    }
}
